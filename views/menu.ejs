<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
      <%-include("__header.ejs")%>
       <div class="container mt-5">
  <div class="row justify-content-center">
    <% for(let product of products) { %>
      <div class="col-md-6 col-lg-3 p-3">
        <div class="card shadow-sm" style="border: 2px solid #ff9800; border-radius: 20px; height: 400px;">
          
          <img src="<%= product.image %>" class="card-img-top" style="height: 200px; object-fit: cover; border-top-left-radius: 20px; border-top-right-radius: 20px;" />
          
          <div class="card-body text-center d-flex flex-column justify-content-between">
            <h5 class="card-title"><%= product.name %></h5>
            <p class="card-text text-success fw-bold"><%= product.price %> ₹</p>
            <a href="/product/<%=product.id%>" class="btn btn-warning w-100 text-white fw-semibold" style="border-radius: 10px;">
              🍽️ View More
            </a>
          </div>

        </div>
      </div>
    <% } %>
  </div>
</div>

      <%-include("__footer.ejs")%>
</body>
</html>